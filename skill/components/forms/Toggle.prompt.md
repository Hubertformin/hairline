A switch, on its own or as a settings row.

```jsx
<Toggle on={on} onChange={flip} label="Read mobile-money messages" note="Parsed on the device; nothing is uploaded" />
```

Always write the note as a consequence, not a restatement of the label.
