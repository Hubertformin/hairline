A labelled field. Use `size="lg"` for the single amount the form exists to collect.

```jsx
<Field label="Amount" prefix="XAF" size="lg" value="8 600" />
<Field label="Note" value="Weekly market run" hint="Optional" />
```

Never add a border — the grey fill is the control. Pair with `Select` for choices.
