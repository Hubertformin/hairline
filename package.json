{
  "name": "ledger-monorepo",
  "private": true,
  "type": "module",
  "description": "Ledger — a calm finance design system and component library.",
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.25.0"
}
